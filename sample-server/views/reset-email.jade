doctype html
html(lang='en')

  head
    meta(charset="UTF-8")
    
    title
      | Password Reset Link
  
  body
    
    h3 Hello, #{user.name}!

    p
      | You've requested a password reset from INSERT_SITE_NAME_HERE.

    p
      | Activation Link: 

      a(href='http://127.0.0.1:8080/cockpit/#/reset/email/' + user.email + '/temp-auth/' + user.tempAuth)
        | Click here.

    p
      | If you're unable to click the link, copy and paste the following URL in your browser:

    p
      pre
        | http://127.0.0.1:8080/cockpit/#/reset/email/#{user.email}/temp-auth/#{user.tempAuth}

    p 
      small
        | If you did not request an reset link, simply ignore this email.  This link will 
        | expire in 24 hours.  Contact your administrator with any questions.
